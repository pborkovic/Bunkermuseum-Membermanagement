# Production Environment Configuration Template
# Bunkermuseum Member Management Platform
#
# INSTRUCTIONS:
# 1. Copy this file to .env.prod
# 2. Fill in all values with your production credentials
# 3. NEVER commit .env.prod to version control
# 4. Ensure file permissions are restricted: chmod 600 .env.prod

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_VERSION=latest
TZ=Europe/Vienna

# Domain Configuration
DOMAIN_NAME=bunkermuseum.example.com
ENABLE_SSL=false  # Set to true once SSL certificates are configured

# Spring Boot Profile
SPRING_PROFILES_ACTIVE=prod

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DB_NAME=bunkermuseum_prod
DB_USERNAME=bunkermuseum_prod_user
DB_PASSWORD=CHANGE_ME_STRONG_DATABASE_PASSWORD
DB_HOST=postgres
DB_PORT=5432

# HikariCP Connection Pool Settings
HIKARI_MAX_POOL_SIZE=20
HIKARI_MIN_IDLE=5

# JPA Settings
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false

# =============================================================================
# MINIO OBJECT STORAGE
# =============================================================================
# MinIO Root Credentials (Admin Access)
MINIO_ROOT_USER=CHANGE_ME_MINIO_ROOT_USER
MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG_MINIO_ROOT_PASSWORD

# MinIO Application Credentials (used by Spring Boot app)
MINIO_ACCESS_KEY=CHANGE_ME_MINIO_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_ME_STRONG_MINIO_SECRET_KEY
MINIO_BUCKET_NAME=bunkermuseum-files

# MinIO URLs
MINIO_ENDPOINT=http://minio:9000
MINIO_BROWSER_URL=https://minio.bunkermuseum.example.com
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================
# SMTP Server Settings
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=noreply@bunkermuseum.example.com
MAIL_PASSWORD=CHANGE_ME_EMAIL_PASSWORD

# SMTP Authentication & Security
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true

# Email From Address
MAIL_FROM_ADDRESS=noreply@bunkermuseum.example.com
MAIL_FROM_NAME=Bunkermuseum Wurzenpass

# =============================================================================
# GOOGLE reCAPTCHA v2
# =============================================================================
# Get keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here
RECAPTCHA_VERIFY_URL=https://www.google.com/recaptcha/api/siteverify

# =============================================================================
# JAVA / JVM CONFIGURATION
# =============================================================================
# JVM Options (adjust based on available server resources)
# For 8GB server: -XX:MaxRAMPercentage=50.0 (app will use ~4GB max)
# For 16GB server: -XX:MaxRAMPercentage=37.5 (app will use ~6GB max)
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=50.0 -XX:+UseG1GC -XX:+UseStringDeduplication

# =============================================================================
# NGINX CONFIGURATION
# =============================================================================
NGINX_VERSION=latest

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Use strong, unique passwords for all credentials
# 2. Enable SSL/TLS before exposing to the internet
# 3. Regularly rotate passwords and secrets
# 4. Monitor logs for suspicious activity
# 5. Keep all Docker images updated
# 6. Configure firewall rules appropriately
# 7. Enable automated backups

# =============================================================================
# OPTIONAL: MONITORING & LOGGING
# =============================================================================
# Uncomment and configure if you have monitoring infrastructure
# SENTRY_DSN=
# DATADOG_API_KEY=
# ELASTIC_APM_SERVER_URL=
